{
  "name": "opendrive-web-tool",
  "version": "0.1.0",
  "private": true,
  "description": "Web OpenDRIVE editor and validator based on VTS check_map rules",
  "scripts": {
    "build:native": "cmake -S native -B native/build && cmake --build native/build --target odr_json_parser -j 4",
    "dev": "vite",
    "dev:server": "BACKEND_PORT=5174 node src/server.js",
    "dev:first": "bash ./scripts/first-run-dev.sh",
    "dev:all": "./scripts/dev.sh",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node src/server.js"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^5.4.0"
  }
}
